import 'package:flutter/material.dart';
import 'package:meet_your_mates/screens/ProjectsSudent/components/body.dart';

class ProjectsStudent extends StatelessWidget {
  const ProjectsStudent({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
